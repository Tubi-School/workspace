# --- TUBI Workspace web ------------------------------------------------------
# Copy to `.env.local` and adjust. `.env.local` is git-ignored.

# Base URL of the TUBI Workspace API.
# Anything prefixed NEXT_PUBLIC_ is inlined into the browser bundle at build
# time, so it must never hold a secret.
NEXT_PUBLIC_API_URL=http://localhost:3001
