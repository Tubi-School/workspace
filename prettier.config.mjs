import baseConfig from '@tubi/config/prettier';

/**
 * Root Prettier configuration.
 *
 * The single source of truth lives in `packages/config` so that every app and
 * package formats identically. Editors pick this file up automatically.
 *
 * @type {import('prettier').Config}
 */
export default baseConfig;
